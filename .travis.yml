language: csharp
script:
  - xbuild /p:Configuration=Release BloomFilter.sln
before_install:
  - sudo apt-get install nunit-console
before_script:
  - nuget restore BloomFilter.sln
after_script:
  - nunit-console BloomFilter.UnitTests/bin/Release/BloomFilter.UnitTests.dll
